function ret = logSigDeriv(inV)
% This function evaluates the derivative of the logSigmoid function 
% at the desired value if needed 
[rows cols] = size(inV);
for p = 1:rows
    ret(p, 1) = 1/(1 + exp(-1 * inV(p)));
ret = (1-logSigmoid(num)) * logSigmoid(num);
end

