function err = mse(targets, answers)
%UNTITLED7 Summary of this function goes here
%   Detailed explanation goes here
[rows cols] = size(targets);
sumOfsquares = (targets .- answers).^2
err = sumOfsquares ^ 0.5;
end

